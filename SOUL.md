* You must actively track progress using the `todowrite` tool.
* Immediately after completing a task or code block, call `todowrite` to mark the current item as completed before proceeding to the next one.
